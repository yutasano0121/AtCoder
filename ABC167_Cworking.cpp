#include <iostream>
#include <vector>
#include <bitset>

int main(){
  int N, M, X;
  std::cin >> N >> M >> X;
  vector<vector<int>> list(N, vector<int>(M));
  for (int i = 0; i < N; i++){
    for (int j = 0; j <= M; j ++){
      cin >> list[i][j];
    }
  }

  for (int i = 1; i <= (1 << N); i++){
    for
  }


  return 0;
}
